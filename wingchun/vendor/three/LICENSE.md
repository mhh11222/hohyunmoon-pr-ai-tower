# vendor/three

`GLTFLoader.js`, `BufferGeometryUtils.js`, `SkeletonUtils.js` are copied from three.js r0.185
(`examples/jsm/`), MIT License (see ../../../mahjong/vendor/three-LICENSE.txt). Only the import
paths were changed so they resolve without an import map (CSP `script-src 'self'`).
